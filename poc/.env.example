# PoC 環境變數範例
# 請複製此檔為 `.env` 並填入適當值（在本地測試時）。

# 若不設定 MERAKI_API_URL / MERAKI_API_KEY，Lambda handler 只會解析並 log，不會發出外部請求。
MERAKI_API_URL=
MERAKI_API_KEY=

# 若要測試把事件轉為 Prometheus metric 並推送至 pushgateway，設定此項（例如 http://localhost:9091）
PROMETHEUS_PUSHGATEWAY=
